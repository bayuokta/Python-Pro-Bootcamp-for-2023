# Type Error, Type Checking an Type Conversion

num_char = str(len(input('Siapa nama kamu ? ')))

print(num_char+' merupakan panjang karakter nama kamu.')

# Type Conversion (tipe data primitif immutable data types)
str()  # digunakan untuk menkonversi ke tipe data string ('Bayu')
float()  # digunakan untuk menkonversi ke tipe data float (1.74)
int()  # digunakan untuk menkonversi ke tipe data int (23)
bool()  # digunakan untuk menkonversi ke tipe data bool (True, False)
